import 'package:flutter/material.dart';

class TambahCustomerPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Tambah Customer'),
      ),
      body: Center(
        child: Text('Halaman Tambah Customer'),
      ),
    );
  }
}
