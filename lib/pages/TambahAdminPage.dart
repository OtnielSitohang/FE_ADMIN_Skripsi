import 'package:flutter/material.dart';

class TambahAdminPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Tambah Admin'),
      ),
      body: Center(
        child: Text('Halaman Tambah Admin'),
      ),
    );
  }
}
