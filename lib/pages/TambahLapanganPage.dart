import 'package:flutter/material.dart';

class TambahLapanganPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Tambah Lapangan'),
      ),
      body: Center(
        child: Text('Halaman Tambah Lapangan'),
      ),
    );
  }
}
